#pragma once

namespace setting
{
	int windowWidth = 800;
	int windowHeight = 600;
}

namespace playerKey
{
	int Player1CommandList[] = { 
		VK_UP, VK_DOWN, VK_LEFT, VK_RIGHT, VK_A, VK_S, VK_D, VK_F
	};
}